jdk:
  - openjdk11
before_install:
  - sdk install gradle 8.0
  - sdk use gradle 8.0
install:
  - echo "Running a custom install command"
  - chmod +x gradlew
  - ./gradlew clean
  - ./gradlew shadowJar